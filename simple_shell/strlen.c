#include "shell.h"

/**
 * _strlen - The function calculates the length of a given string
 *
 * @s - The particular string to find length of
 *@i - length of the string
 * Returns: The length of the string or -1 if string is not found
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		/* empty body */
	}

	return (i);
}

